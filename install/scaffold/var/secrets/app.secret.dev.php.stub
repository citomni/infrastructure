<?php
declare(strict_types=1);
/*
 * CitOmni - Application secrets - DEV
 *
 * ENVIRONMENT
 *   DEV only.
 *   Runtime file: var/secrets/app.secret.dev.php
 *
 * PURPOSE
 *   Store private application credentials for the DEV environment.
 *
 * SECURITY
 *   - This scaffold contains no real secrets.
 *   - Never commit the populated runtime file.
 *   - Do not place stage or production credentials in this file.
 *   - Keep non-secret runtime policy and configuration in $app->cfg.
 *   - Restrict filesystem access where supported (0600 is recommended on Unix-like systems).
 *
 * CONTRACT
 *   - Return a flat array<string,string>.
 *   - Keys are exact and case-sensitive.
 *   - Dotted keys are recommended for readable namespacing.
 */

return [
	// 'db.password' => 'REPLACE-WITH-DEV-DATABASE-PASSWORD',
	// 'mail.smtp.password' => 'REPLACE-WITH-DEV-SMTP-PASSWORD',
	// 'service.api_token' => 'REPLACE-WITH-DEV-API-TOKEN',
];
