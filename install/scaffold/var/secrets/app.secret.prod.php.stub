<?php
declare(strict_types=1);
/*
 * CitOmni - Application secrets - PROD
 *
 * ENVIRONMENT
 *   PROD only.
 *   Runtime file: var/secrets/app.secret.prod.php
 *
 * PURPOSE
 *   Store private application credentials for the PROD environment.
 *
 * SECURITY
 *   - This scaffold contains no real secrets.
 *   - Never commit the populated runtime file.
 *   - Do not place development or staging credentials in this file.
 *   - Keep non-secret runtime policy and configuration in $app->cfg.
 *   - Restrict filesystem access where supported (0600 is recommended on Unix-like systems).
 *
 * CONTRACT
 *   - Return a flat array<string,string>.
 *   - Keys are exact and case-sensitive.
 *   - Dotted keys are recommended for readable namespacing.
 */

return [
	// 'db.password' => 'REPLACE-WITH-PROD-DATABASE-PASSWORD',
	// 'mail.smtp.password' => 'REPLACE-WITH-PROD-SMTP-PASSWORD',
	// 'service.api_token' => 'REPLACE-WITH-PROD-API-TOKEN',
];
