<?php
declare(strict_types=1);
/*
 * CitOmni - Application secrets template
 *
 * PURPOSE
 *   Provide the expected structure for the private application secret store used
 *   by CitOmni\Infrastructure\Service\Secrets.
 *
 * SECURITY
 *   - This template contains no real secrets and may be committed to version control.
 *   - Never commit the populated secret file.
 *   - Keep runtime credentials here instead of in /config.
 *   - Restrict filesystem access to the populated secret file where supported
 *     (0600 is recommended on Unix-like systems).
 *
 * CONTRACT
 *   - Copy this template to var/secrets/app.secret.php before adding real values.
 *   - The populated file must return a flat array<string,string>.
 *   - Keys are exact, case-sensitive identifiers.
 *   - Dotted keys are recommended for readable namespacing.
 *   - Keep non-secret runtime policy and configuration in $app->cfg.
 *
 * TYPICAL TARGET
 *   var/secrets/app.secret.php
 */

return [
	// 'db.password' => 'REPLACE-WITH-DATABASE-PASSWORD',
	// 'mail.smtp.password' => 'REPLACE-WITH-SMTP-PASSWORD',
	// 'service.api_token' => 'REPLACE-WITH-API-TOKEN',
];
